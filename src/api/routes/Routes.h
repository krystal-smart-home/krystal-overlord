#pragma once

namespace krystal::api::routes {

class Routes {
protected:
/*    void sendJson(ResponseWriter& res) {*/
        //res.headers().add<Pistache::Http::Header::ContentType>(MIME(Application, Json));
        //res.send(code, body);
/*    }*/
};
}
