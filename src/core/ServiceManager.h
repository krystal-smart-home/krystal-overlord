#pragma once

#include <memory>
#include <vector>

#include "Service.h"

namespace krystal::core {

class ServiceManager {
public:
    void registerService(std::shared_ptr<Service> service) {
    }

private:
};
}
